[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = { version = "0.14.4", default-features = false, features = ["derive"] }

anybuf = "1.0.0"
cosmwasm-std = { workspace = true, features = ["cosmwasm_2_0", "staking"] }
